# currency-converter
A real-time currency converter built with Python Flask. Fetches live exchange rates for 160+ currencies via the Open Exchange Rates API — no API key required. Features server-side caching, swap functionality, and a clean dark UI. Deployed on Railway.
